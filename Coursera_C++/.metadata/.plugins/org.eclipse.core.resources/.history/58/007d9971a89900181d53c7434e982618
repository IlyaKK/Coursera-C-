#include <iostream>
#include <vector>
using namespace std;

int main() {
	int a,i;
	cin >> a;
	vector<int> v;
	vector<int> x;
	x = {a/2};
	v = {a-2*x};
	cout<<x;
	/*while(v[i] != 0){
		x.push_back(x[i]/2);
		v.push_back(x[i]-2*x[i+1]);
		i++;
	}*/
	return 0;
}

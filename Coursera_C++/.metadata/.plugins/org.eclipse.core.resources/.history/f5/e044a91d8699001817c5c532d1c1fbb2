#include <iostream>
#include <vector>
using namespace std;

int main() {
	int a,i;
	cin >> a;
	vector<int> v;
	vector<int> x = {a/2};
	i=0;
	v[i] = a-2*x[i];
	cout<<x;
	/*while(v[i] != 0){
		x.push_back(x[i]/2);
		v.push_back(x[i]-2*x[i+1]);
		i++;
	}*/
	return 0;
}
